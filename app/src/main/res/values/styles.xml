<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:scrollbarThumbVertical">@drawable/colorfull_scrollbar</item>
    </style>

</resources>
